version: '3'

tasks:
  sed:
    requires:
      vars:
        - FROM
        - TO
        - FILE
    desc: Changes a file with sed for Mac.
    cmd: sed -i '' "s/{{.FROM}}/{{.TO}}/" {{.FILE}}

  browser:
    requires:
      vars:
        - TARGET
    cmd: open {{.TARGET}}
